//
// Class16DayDailyForecastAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech

import Foundation
import Combine
import OpenAPITransport


open class Class16DayDailyForecastAPI {
    private let transport: OpenAPITransport
    public var encoder: JSONEncoder = {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .formatted(OpenISO8601DateFormatter())
        return encoder
    }()
    public var decoder: JSONDecoder = {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .formatted(OpenISO8601DateFormatter())
        return decoder
    }()
    public var baseURL = URL(string: "https://api.weatherbit.io/v2.0")

    public init(_ transport: OpenAPITransport) {
        self.transport = transport
    }

    ///
    /// Enum for parameter units
    ///
    public enum ForecastDailycityIdcityIdGetUnits: String, Codable, CaseIterable {
        case s = "S"
        case i = "I"
    }
    ///
    /// Enum for parameter lang
    ///
    public enum ForecastDailycityIdcityIdGetLang: String, Codable, CaseIterable {
        case ar = "ar"
        case az = "az"
        case be = "be"
        case bg = "bg"
        case bs = "bs"
        case ca = "ca"
        case cs = "cs"
        case de = "de"
        case fi = "fi"
        case fr = "fr"
        case el = "el"
        case es = "es"
        case et = "et"
        case hr = "hr"
        case hu = "hu"
        case id = "id"
        case it = "it"
        case _is = "is"
        case kw = "kw"
        case nb = "nb"
        case nl = "nl"
        case pl = "pl"
        case pt = "pt"
        case ro = "ro"
        case ru = "ru"
        case sk = "sk"
        case sl = "sl"
        case sr = "sr"
        case sv = "sv"
        case tr = "tr"
        case uk = "uk"
        case zh = "zh"
        case zhTw = "zh-tw"
    }

    /// Returns a daily forecast - Given a City ID.
    /// - GET /forecast/daily?city_id={city_id}
    /// - Returns a daily forecast, where each point represents one day (24hr) period. Every point has a datetime string in the format \"YYYY-MM-DD\". One day begins at 00:00 UTC, and ends at 23:59 UTC. 
    /// - parameter cityId: (path) City ID. Example: 4487042 
    /// - parameter key: (query) Your registered API key. 
    /// - parameter days: (query) Number of days to return. Default 16. (optional)
    /// - parameter units: (query) Convert to units. Default Metric See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;units field description&lt;/a&gt; (optional)
    /// - parameter lang: (query) Language (Default: English) See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;language field description&lt;/a&gt; (optional)
    /// - parameter callback: (query) Wraps return in jsonp callback. Example: callback&#x3D;func (optional)
    /// - returns: AnyPublisher<ForecastDay, Error> 
    open func forecastDailycityIdcityIdGet(cityId: Int, key: String, days: Double? = nil, units: ForecastDailycityIdcityIdGetUnits? = nil, lang: ForecastDailycityIdcityIdGetLang? = nil, callback: String? = nil) -> AnyPublisher<ForecastDay, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                var path = "/forecast/daily?city_id&#x3D;{city_id}"
                path = path.replacingOccurrences(of: "{city_id}", with: "\(cityId)")
                let url = baseURL.appendingPathComponent(path)
                var components = URLComponents(url: url, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                if let days = days { queryItems.append(URLQueryItem(name: "days", value: "\(days)")) } 
                if let units = units { queryItems.append(URLQueryItem(name: "units", value: units.rawValue)) } 
                if let lang = lang { queryItems.append(URLQueryItem(name: "lang", value: lang.rawValue)) } 
                if let callback = callback { queryItems.append(URLQueryItem(name: "callback", value: callback)) } 
                queryItems.append(URLQueryItem(name: "key", value: key))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<ForecastDay, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(ForecastDay.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }

    ///
    /// Enum for parameter units
    ///
    public enum ForecastDailycitycitycountrycountryGetUnits: String, Codable, CaseIterable {
        case s = "S"
        case i = "I"
    }
    ///
    /// Enum for parameter lang
    ///
    public enum ForecastDailycitycitycountrycountryGetLang: String, Codable, CaseIterable {
        case ar = "ar"
        case az = "az"
        case be = "be"
        case bg = "bg"
        case bs = "bs"
        case ca = "ca"
        case cs = "cs"
        case de = "de"
        case fi = "fi"
        case fr = "fr"
        case el = "el"
        case es = "es"
        case et = "et"
        case hr = "hr"
        case hu = "hu"
        case id = "id"
        case it = "it"
        case _is = "is"
        case kw = "kw"
        case nb = "nb"
        case nl = "nl"
        case pl = "pl"
        case pt = "pt"
        case ro = "ro"
        case ru = "ru"
        case sk = "sk"
        case sl = "sl"
        case sr = "sr"
        case sv = "sv"
        case tr = "tr"
        case uk = "uk"
        case zh = "zh"
        case zhTw = "zh-tw"
    }

    /// Returns a daily forecast - Given City and/or State, Country.
    /// - GET /forecast/daily?city={city}&country={country}
    /// - Returns a daily forecast, where each point represents one day (24hr) period. Every point has a datetime string in the format \"YYYY-MM-DD\". One day begins at 00:00 UTC, and ends at 23:59 UTC. Accepts a city in the format of City,ST or City. The state, and country parameters can be provided to make the search more accurate. 
    /// - parameter city: (path) City search.. Example - &amp;city&#x3D;Raleigh,NC or &amp;city&#x3D;Berlin,DE or city&#x3D;Paris&amp;country&#x3D;FR 
    /// - parameter country: (path) Country Code (2 letter). 
    /// - parameter key: (query) Your registered API key. 
    /// - parameter state: (query) Full name of state. (optional)
    /// - parameter days: (query) Number of days to return. Default 16. (optional)
    /// - parameter units: (query) Convert to units. Default Metric See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;units field description&lt;/a&gt; (optional)
    /// - parameter lang: (query) Language (Default: English) See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;language field description&lt;/a&gt; (optional)
    /// - parameter callback: (query) Wraps return in jsonp callback. Example - callback&#x3D;func (optional)
    /// - returns: AnyPublisher<ForecastDay, Error> 
    open func forecastDailycitycitycountrycountryGet(city: String, country: String, key: String, state: String? = nil, days: Double? = nil, units: ForecastDailycitycitycountrycountryGetUnits? = nil, lang: ForecastDailycitycitycountrycountryGetLang? = nil, callback: String? = nil) -> AnyPublisher<ForecastDay, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                var path = "/forecast/daily?city&#x3D;{city}&amp;country&#x3D;{country}"
                path = path.replacingOccurrences(of: "{city}", with: city)
                path = path.replacingOccurrences(of: "{country}", with: country)
                let url = baseURL.appendingPathComponent(path)
                var components = URLComponents(url: url, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                if let state = state { queryItems.append(URLQueryItem(name: "state", value: state)) } 
                if let days = days { queryItems.append(URLQueryItem(name: "days", value: "\(days)")) } 
                if let units = units { queryItems.append(URLQueryItem(name: "units", value: units.rawValue)) } 
                if let lang = lang { queryItems.append(URLQueryItem(name: "lang", value: lang.rawValue)) } 
                if let callback = callback { queryItems.append(URLQueryItem(name: "callback", value: callback)) } 
                queryItems.append(URLQueryItem(name: "key", value: key))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<ForecastDay, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(ForecastDay.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }

    ///
    /// Enum for parameter units
    ///
    public enum ForecastDailylatlatlonlonGetUnits: String, Codable, CaseIterable {
        case s = "S"
        case i = "I"
    }
    ///
    /// Enum for parameter lang
    ///
    public enum ForecastDailylatlatlonlonGetLang: String, Codable, CaseIterable {
        case ar = "ar"
        case az = "az"
        case be = "be"
        case bg = "bg"
        case bs = "bs"
        case ca = "ca"
        case cs = "cs"
        case de = "de"
        case fi = "fi"
        case fr = "fr"
        case el = "el"
        case es = "es"
        case et = "et"
        case hr = "hr"
        case hu = "hu"
        case id = "id"
        case it = "it"
        case _is = "is"
        case kw = "kw"
        case nb = "nb"
        case nl = "nl"
        case pl = "pl"
        case pt = "pt"
        case ro = "ro"
        case ru = "ru"
        case sk = "sk"
        case sl = "sl"
        case sr = "sr"
        case sv = "sv"
        case tr = "tr"
        case uk = "uk"
        case zh = "zh"
        case zhTw = "zh-tw"
    }

    /// Returns a daily forecast - Given Lat/Lon.
    /// - GET /forecast/daily?lat={lat}&lon={lon}
    /// - Returns a daily forecast, where each point represents one day (24hr) period. Every point has a datetime string in the format \"YYYY-MM-DD\". One day begins at 00:00 UTC, and ends at 23:59 UTC.  
    /// - parameter lat: (path) Latitude component of location. 
    /// - parameter lon: (path) Longitude component of location. 
    /// - parameter key: (query) Your registered API key. 
    /// - parameter days: (query) Number of days to return. Default 16. (optional)
    /// - parameter units: (query) Convert to units. Default Metric See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;units field description&lt;/a&gt; (optional)
    /// - parameter lang: (query) Language (Default: English) See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;language field description&lt;/a&gt; (optional)
    /// - parameter callback: (query) Wraps return in jsonp callback. Example: callback&#x3D;func (optional)
    /// - returns: AnyPublisher<ForecastDay, Error> 
    open func forecastDailylatlatlonlonGet(lat: Double, lon: Double, key: String, days: Double? = nil, units: ForecastDailylatlatlonlonGetUnits? = nil, lang: ForecastDailylatlatlonlonGetLang? = nil, callback: String? = nil) -> AnyPublisher<ForecastDay, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                var path = "/forecast/daily?lat&#x3D;{lat}&amp;lon&#x3D;{lon}"
                path = path.replacingOccurrences(of: "{lat}", with: "\(lat)")
                path = path.replacingOccurrences(of: "{lon}", with: "\(lon)")
                let url = baseURL.appendingPathComponent(path)
                var components = URLComponents(url: url, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                if let days = days { queryItems.append(URLQueryItem(name: "days", value: "\(days)")) } 
                if let units = units { queryItems.append(URLQueryItem(name: "units", value: units.rawValue)) } 
                if let lang = lang { queryItems.append(URLQueryItem(name: "lang", value: lang.rawValue)) } 
                if let callback = callback { queryItems.append(URLQueryItem(name: "callback", value: callback)) } 
                queryItems.append(URLQueryItem(name: "key", value: key))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<ForecastDay, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(ForecastDay.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }

    ///
    /// Enum for parameter units
    ///
    public enum ForecastDailypostalCodepostalCodeGetUnits: String, Codable, CaseIterable {
        case s = "S"
        case i = "I"
    }
    ///
    /// Enum for parameter lang
    ///
    public enum ForecastDailypostalCodepostalCodeGetLang: String, Codable, CaseIterable {
        case ar = "ar"
        case az = "az"
        case be = "be"
        case bg = "bg"
        case bs = "bs"
        case ca = "ca"
        case cs = "cs"
        case de = "de"
        case fi = "fi"
        case fr = "fr"
        case el = "el"
        case es = "es"
        case et = "et"
        case hr = "hr"
        case hu = "hu"
        case id = "id"
        case it = "it"
        case _is = "is"
        case kw = "kw"
        case nb = "nb"
        case nl = "nl"
        case pl = "pl"
        case pt = "pt"
        case ro = "ro"
        case ru = "ru"
        case sk = "sk"
        case sl = "sl"
        case sr = "sr"
        case sv = "sv"
        case tr = "tr"
        case uk = "uk"
        case zh = "zh"
        case zhTw = "zh-tw"
    }

    /// Returns a daily forecast - Given a Postal Code.
    /// - GET /forecast/daily?postal_code={postal_code}
    /// - Returns a daily forecast, where each point represents one day (24hr) period. Every point has a datetime string in the format \"YYYY-MM-DD\". One day begins at 00:00 UTC, and ends at 23:59 UTC. 
    /// - parameter postalCode: (path) Postal Code. Example: 28546 
    /// - parameter key: (query) Your registered API key. 
    /// - parameter country: (query) Country Code (2 letter). (optional)
    /// - parameter days: (query) Number of days to return. Default 16. (optional)
    /// - parameter units: (query) Convert to units. Default Metric See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;units field description&lt;/a&gt; (optional)
    /// - parameter lang: (query) Language (Default: English) See &lt;a target&#x3D;&#39;blank&#39; href&#x3D;&#39;/api/requests&#39;&gt;language field description&lt;/a&gt; (optional)
    /// - parameter callback: (query) Wraps return in jsonp callback. Example: callback&#x3D;func (optional)
    /// - returns: AnyPublisher<ForecastDay, Error> 
    open func forecastDailypostalCodepostalCodeGet(postalCode: Int, key: String, country: String? = nil, days: Double? = nil, units: ForecastDailypostalCodepostalCodeGetUnits? = nil, lang: ForecastDailypostalCodepostalCodeGetLang? = nil, callback: String? = nil) -> AnyPublisher<ForecastDay, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                var path = "/forecast/daily?postal_code&#x3D;{postal_code}"
                path = path.replacingOccurrences(of: "{postal_code}", with: "\(postalCode)")
                let url = baseURL.appendingPathComponent(path)
                var components = URLComponents(url: url, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                if let country = country { queryItems.append(URLQueryItem(name: "country", value: country)) } 
                if let days = days { queryItems.append(URLQueryItem(name: "days", value: "\(days)")) } 
                if let units = units { queryItems.append(URLQueryItem(name: "units", value: units.rawValue)) } 
                if let lang = lang { queryItems.append(URLQueryItem(name: "lang", value: lang.rawValue)) } 
                if let callback = callback { queryItems.append(URLQueryItem(name: "callback", value: callback)) } 
                queryItems.append(URLQueryItem(name: "key", value: key))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<ForecastDay, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(ForecastDay.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }
}
